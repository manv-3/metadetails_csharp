

const Home = () => {
    return (
        <div>
            <h1>MetaDetective Online</h1>
            <p>Upload files or scrape a URL to extract rich metadata for OSINT and security research.</p>
        </div>
    );
};

export default Home;
